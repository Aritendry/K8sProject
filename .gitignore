# Clés privées / certificats (jamais versionnés en prod ; la démo embarque
# le secret dans tls.yaml, donc on ignore les fichiers sources openssl).
*.key
*.crt

# Script de lancement personnel (peut contenir des tokens) — à ne pas versionner.
cmd

# Python / Airflow
__pycache__/
*.py[cod]
